apply plugin: "war"

bootWar {
    mainClass = "com.kepco.etax.api.KepcoEtaxApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
